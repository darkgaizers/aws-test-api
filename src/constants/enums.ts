export enum HttpLogTypeEnum {
  exception = 'exception',
}

export enum CompanyGroupTypeEnum {
  company = 'company',
  group = 'group',
}

export enum UserRoleEnum {
  assessor = 'Assesor',
  categoryExpert = 'Category Expert',
  internalOwner = 'Internal Owner',
  groupAnswer = 'Group Admin',
}

export enum RiskTypeEnum {
  item = 'item',
  summary = 'summary',
}

export enum AssessmentRateEnum {
  zero = 0,
  one = 1,
  two = 2,
  three = 3,
  four = 4,
  five = 5,
}

export enum AssessmentTypeEnum {
  gross = 'gross',
  net = 'net',
}

export enum HeatMapIndicatorEnum {
  L = 'L',
  M = 'M',
  H = 'H',
}

export enum EmailType {
  resetPassword = 'reset_password',
}

export enum ReviewStatusEnum {
  approved = 'approved',
  rejected = 'rejected',
  waiting = 'waiting',
}
